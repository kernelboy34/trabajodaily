@page
@model follows.Pages.panel.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>docentes</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="docentes.Id" />
            <div class="form-group">
                <label asp-for="docentes.Name" class="control-label"></label>
                <input asp-for="docentes.Name" class="form-control" />
                <span asp-validation-for="docentes.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="docentes.course" class="control-label"></label>
                <input asp-for="docentes.course" class="form-control" />
                <span asp-validation-for="docentes.course" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
